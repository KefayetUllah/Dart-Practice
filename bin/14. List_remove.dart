void main(){
  var numbers = [1, 2, 3,4,5,6];

  numbers.removeLast();  // remove last index
  numbers.removeAt(2);   //for removing index 2
  numbers.remove(5);  //for removing value 5 from the list

  print(numbers);
}